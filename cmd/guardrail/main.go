// Command guardrail is the CLI/server entry point for the AI agent
// guardrail & policy engine. Subcommands (serve, validate, tail-audit) are
// added in later phases.
package main

import "fmt"

func main() {
	fmt.Println("guardrail: scaffolding only, no subcommands yet")
}
